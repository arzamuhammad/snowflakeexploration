CREATE TABLE testing.ocs_chg_reject_poc_tokenized (
  timestamp_r TIMESTAMP,
  trx_date STRING,
  trx_hour TINYINT,
  a_party STRING,
  account_owner STRING,
  msisdn STRING,
  pre_post_flag TINYINT,
  subs_id STRING,
  location_number_type_a STRING,
  location_number_a STRING,
  b_party STRING,
  location_number_type_b STRING,
  location_number_b STRING,
  number_of_reroutings STRING,
  imsi STRING,
  number_of_network_reroutings STRING,
  redirecting_party_id STRING,
  pre_call_duration INT,
  call_duration INT,
  service_filter STRING,
  vas_code STRING,
  content_id STRING,
  charging_id STRING,
  pack_id STRING,
  activation_channel_id STRING,
  zone STRING,
  pack_version STRING,
  purchase_num STRING,
  roaming_flag SMALLINT,
  cost_band STRING,
  tsel_cost_band STRING,
  l3_name STRING,
  l2_name STRING,
  l1_name STRING,
  data_user TINYINT,
  data_usage_type STRING,
  dls_charge_flag STRING,
  vlr_num STRING,
  cell_id STRING,
  lacci_id STRING,
  lacci_closing_flag TINYINT,
  lac STRING,
  ci STRING,
  node_type STRING,
  area_sales STRING,
  region_sales STRING,
  branch STRING,
  subbranch STRING,
  cluster_sales STRING,
  provinsi STRING,
  kabupaten STRING,
  kecamatan STRING,
  kelurahan STRING,
  negotiated_qos STRING,
  requested_qos STRING,
  subscribed_qos STRING,
  dialled_apn STRING,
  event_type STRING,
  provider_id BIGINT,
  offer STRING,
  brand STRING,
  cust_type_desc STRING,
  cust_subtype_desc STRING,
  currency STRING,
  subscriber_status STRING,
  forwarding_flag SMALLINT,
  first_call_flag SMALLINT,
  camel_roaming SMALLINT,
  time_zone SMALLINT,
  account_balance DECIMAL(18,2),
  account_number STRING,
  event_source STRING,
  guiding_resource_type STRING,
  rounded_duration BIGINT,
  total_volume BIGINT,
  rounded_total_volume BIGINT,
  event_start_period SMALLINT,
  cifa DECIMAL(18,2),
  account_delta DECIMAL(18,2),
  bonus_consumed STRING,
  credit_indicator STRING,
  rev DECIMAL(18,2),
  charge_code STRING,
  discount_amount DECIMAL(18,2),
  free_total_volume BIGINT,
  free_duration BIGINT,
  call_direction STRING,
  special_number_ind TINYINT,
  bonus_information STRING,
  internal_cause STRING,
  basic_cause STRING,
  time_band STRING,
  call_type STRING,
  national_calling_zone STRING,
  national_called_zone STRING,
  international_calling_zone STRING,
  international_called_zone STRING,
  event_id STRING,
  access_code STRING,
  country_name STRING,
  cp_name STRING,
  rating_group STRING,
  bft_indicator STRING,
  unapplied_amount DECIMAL(18,2),
  partition_id SMALLINT,
  rated_date_time TIMESTAMP,
  area STRING,
  charge_code_description STRING,
  moc_classification STRING,
  rating_offer_id BIGINT,
  settlement_indicator SMALLINT,
  tap_code STRING,
  mcc_mnc STRING,
  mcc STRING,
  mnc STRING,
  rating_pricing_item_id STRING,
  file_identifier BIGINT,
  record_number BIGINT,
  future_string1 STRING,
  future_string2 STRING,
  imei STRING,
  device_id STRING,
  network_activity_id STRING,
  operator_name STRING,
  node STRING,
  retry_count DECIMAL(18,2),
  retry_ts TIMESTAMP,
  file_id STRING,
  load_ts TIMESTAMP,
  load_user STRING,
  region_hlr STRING,
  city_hlr STRING,
  site_name STRING,
  external_id STRING,
  cust_id INT,
  customer_sub_segment STRING,
  site_id STRING,
  site_id_ifrs STRING,
  lacci_id_ifrs STRING,
  lacci_closing_flag_ifrs TINYINT,
  payu_ifrs_flag BOOLEAN,
  channel_name STRING,
  nbp_flag STRING,
  event_date STRING)
USING parquet
PARTITIONED BY (event_date)
LOCATION 'hdfs://nsdiscovery/data/landing/gd_it_bdm_ops/fastdb/bir/ocs_chg_reject_poc_tokenized'
TBLPROPERTIES (
  'bucketing_version' = '2',
  'transient_lastDdlTime' = '1744596394')
